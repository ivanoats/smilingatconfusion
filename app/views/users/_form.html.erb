<%= simple_form_for @user do |f| %>
  <%= f.input :name, label: "Username" %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.button :submit, params[:action] == "new" ? "Sign Up" : nil %>
<% end %>
